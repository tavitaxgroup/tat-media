# Agent Rules

- Do NOT perform git commit, git push, or deploy commands automatically.
- Only make changes to the local workspace files and let the user handle committing, pushing to GitHub, and deploying (e.g. to Vercel).
